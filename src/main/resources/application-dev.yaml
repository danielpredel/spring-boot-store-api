spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/${DB_NAME}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
